general {
    lock_cmd = hyprlock
    before_sleep_cmd = log out before suspend
    after_sleep_cmd = hyprctl dispatch dpms on
}

listener {
    timeout = 1800
    on-timeout = hyprctl dispatch dpms off && hyprlock
    on-resume = hyprctl dispatch dpms on
}
